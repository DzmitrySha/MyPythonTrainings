# Функция перевода градусов Цельсия в Фаренгейт
# Celsius to Fahrenheit conversion function

celsius = int(input())

def conv(c):
    return (9 / 5 * c + 32)
fahrenheit = conv(celsius)

print(fahrenheit)
